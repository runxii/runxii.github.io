export function WorkSection() {
    return (
        <section id="work" className="px-6 py-24">
            <div className="mx-auto max-w-6xl">
                <p className="text-xs font-medium uppercase tracking-[0.18em] opacity-70">
                    Work
                </p>
                <h2 className="mt-4 text-4xl font-extrabold tracking-[-0.04em] md:text-5xl">
                    Projects (preview)
                </h2>
                <p className="mt-3 text-sm opacity-75">
                    We’ll embed the role-filtered grid here later, matching the /work page.
                </p>
            </div>
        </section>
    );
}
